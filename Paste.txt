{/* Stats row */}
        <div style={{ display: "flex", gap: 40, flexWrap: "wrap" }}>
          {[
            { label: "Distance", val: activeWeek.km, unit: "km", decimals: 1, color: "white" },
            {
              label: "Time",
              val: null,
              display: `${Math.floor(activeWeek.hrs)}h ${Math.round((activeWeek.hrs % 1) * 60)}m`,
              color: "white"
            },
            { label: "Elevation Gain", val: activeWeek.elev, unit: "m", decimals: 0, color: "white" },
            { label: "Coins", val: activeWeek.coins, unit: "🪙", decimals: 0, color: "#FFD600" },
          ].map((s, i) => (
            <motion.div
              key={s.label}
              initial={{ opacity: 0, y: 8 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: i * 0.07, duration: 0.35 }}
            >
              <div style={{
                fontFamily: "'DM Sans', sans-serif",
                fontSize: 13, color: "#9CA3AF", fontWeight: 400, marginBottom: 2,
              }}>
                {s.label}
              </div>
              <div style={{
                fontFamily: "'DM Sans', sans-serif",
                fontSize: 28, fontWeight: 700, color: s.color, lineHeight: 1,
                display: "flex", alignItems: "baseline", gap: 4,
              }}>
                {s.display ? s.display : (
                  <>
                    <AnimCount target={s.val} decimals={s.decimals} />
                    <span style={{ fontSize: 22 }}> {s.unit}</span>
                  </>
                )}
              </div>
            </motion.div>
          ))}
        </div>
      </div>